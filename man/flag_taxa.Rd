% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_taxa.R
\name{flag_taxa}
\alias{flag_taxa}
\title{Flag taxa against expected ecological and geographic context}
\usage{
flag_taxa(
  x,
  expected_environment,
  expected_habitat = NULL,
  expected_region = NULL,
  tax_ranks = c("species", "genus", "family", "order", "class", "phylum", "kingdom"),
  evidence_sources = c("worms", "obis", "gbif", "bold", "literature"),
  taxon_evidence = NULL,
  prompt_only = TRUE,
  verbose = FALSE,
  mock_llm_result = NULL
)
}
\arguments{
\item{x}{A taxonomy table as a data frame, matrix, phyloseq taxonomy table,
or a phyloseq/speedyseq object containing a taxonomy table.}

\item{expected_environment}{Character scalar describing the expected broad
environment, for example \code{"marine"}, \code{"freshwater"}, or \code{"terrestrial"}.}

\item{expected_habitat}{Optional character scalar describing the expected
habitat, for example \code{"estuary"} or \code{"kelp forest"}.}

\item{expected_region}{Optional character scalar describing the expected
geographic region.}

\item{tax_ranks}{Character vector of taxonomic ranks to inspect, ordered from
most specific to broadest.}

\item{evidence_sources}{Character vector naming evidence sources the prompt
should ask the reviewer to consider.}

\item{taxon_evidence}{Optional data frame containing user-supplied evidence
about taxon environment, habitat, and/or region. Required columns are
\code{taxon_name}, \code{taxon_rank}, \code{source}, \code{evidence_type}, \code{evidence_summary},
and \code{reference}. Optional columns are \code{accepted_name}, \code{accepted_rank},
\code{source_taxon_id}, \code{source_record_id}, \code{environment}, \code{habitat}, \code{region},
\code{locality}, \code{decimal_latitude}, \code{decimal_longitude}, \code{basis_of_record},
\code{occurrence_count}, \code{reference_url}, \code{doi}, and \code{checked_at}.}

\item{prompt_only}{Logical scalar. When \code{TRUE}, the generated prompt is
returned as a character scalar. When \code{FALSE}, \code{mock_llm_result} must be
provided because the LLM backend is not implemented yet.}

\item{verbose}{Logical scalar. If \code{TRUE}, emit a short message about the
number of taxa prepared for the prompt.}

\item{mock_llm_result}{Optional data frame used for testing the validation
of an LLM-like result while the backend is not implemented.}
}
\value{
A character scalar containing the generated prompt when
\code{prompt_only = TRUE}, or a validated result table when \code{prompt_only = FALSE}
and \code{mock_llm_result} is provided.
}
\description{
Builds a review prompt for checking whether taxa are compatible with an
expected environment, habitat, and region. This first scaffold does not call
an LLM or query external databases. It only extracts taxonomy, chooses the
most specific useful taxon for each row, and returns the prompt that can be
inspected or copied into a later review workflow. It can also validate a
mocked LLM-like result table while the backend is under development.
}
\section{Taxon evidence table}{

\code{taxon_evidence} can be used to provide curated evidence that should be
considered before any future online evidence lookup. The required columns are:
\code{taxon_name}, \code{taxon_rank}, \code{source}, \code{evidence_type}, \code{evidence_summary},
and \code{reference}.

Optional columns are: \code{accepted_name}, \code{accepted_rank}, \code{source_taxon_id},
\code{source_record_id}, \code{environment}, \code{habitat}, \code{region}, \code{locality},
\code{decimal_latitude}, \code{decimal_longitude}, \code{basis_of_record},
\code{occurrence_count}, \code{reference_url}, \code{doi}, and \code{checked_at}.
}

\examples{
tax <- data.frame(
  kingdom = "Animalia",
  phylum = "Chordata",
  family = "Salmonidae",
  genus = "Salmo",
  species = "Salmo salar"
)

prompt <- flag_taxa(
  tax,
  expected_environment = "marine",
  expected_habitat = "coastal water",
  expected_region = "North Atlantic"
)
}
