% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_worms_ccz_evidence.R
\name{read_worms_ccz_evidence}
\alias{read_worms_ccz_evidence}
\title{Read local WoRMS Clarion-Clipperton Zone checklist evidence}
\usage{
read_worms_ccz_evidence(path, checked_at = Sys.Date())
}
\arguments{
\item{path}{Path to a local CCZ checklist file. Supported file types are
\code{.csv}, \code{.tsv}, \code{.txt}, \code{.rds}, and \code{.xlsx}/\code{.xls} when the optional
\code{readxl} package is installed.}

\item{checked_at}{Date or character scalar recorded in the \code{checked_at}
column.}
}
\value{
A data frame in the standard taxon evidence format, with optional
lineage columns (\code{kingdom}, \code{phylum}, \code{class}, \code{order}, \code{family}, and
\code{genus}) preserved when present in the input.
}
\description{
Reads a locally downloaded WoRMS Clarion-Clipperton Zone (CCZ)
taxlist/checklist file and converts it to the standard \code{taxon_evidence}
format used by \code{flag_taxa()}. This function is a local file reader and
standardiser only: it does not download data, query WoRMS, or call any LLM.

Users must obtain the CCZ checklist separately from WoRMS according to the
WoRMS terms of use. The returned rows use \code{source = "worms_ccz"} and
\code{evidence_type = "regional_deepsea_checklist"} so this regional deep-sea
evidence remains separate from generic WoRMS evidence returned by
\code{fetch_worms_evidence()}. Both evidence types can be combined and passed to
\code{flag_taxa(taxon_evidence = ...)}.
}
\examples{
\dontrun{
ccz_evidence <- read_worms_ccz_evidence("CCZ_taxlist.csv")

taxa_to_query <- extract_taxa_for_evidence(ps_test_data_euk)
worms_evidence <- fetch_worms_evidence(
  taxa_to_query,
  by = "name",
  cache_path = "worms_cache.rds"
)

taxon_evidence <- dplyr::bind_rows(worms_evidence, ccz_evidence)
prompt <- flag_taxa(
  ps_test_data_euk,
  expected_environment = "marine",
  expected_habitat = "deep sea",
  expected_region = "Clarion-Clipperton Zone",
  taxon_evidence = taxon_evidence,
  prompt_only = TRUE
)
}
}
