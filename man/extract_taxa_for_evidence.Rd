% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_taxa_for_evidence.R
\name{extract_taxa_for_evidence}
\alias{extract_taxa_for_evidence}
\title{Extract taxa for evidence retrieval}
\usage{
extract_taxa_for_evidence(
  x,
  ranks = c("Phylum", "Class", "Order", "Family", "Genus", "Species"),
  mode = c("highest", "all"),
  include_feature_id = FALSE,
  drop_uncultured = TRUE,
  drop_empty = TRUE,
  unique = TRUE
)
}
\arguments{
\item{x}{A taxonomy table as a data frame or matrix, a phyloseq taxonomy
table, or a phyloseq/speedyseq object containing a taxonomy table.}

\item{ranks}{Character vector of taxonomy ranks to extract. Rank columns are
matched case-insensitively. The default excludes \code{Domain},
\code{Superkingdom}, and \code{Kingdom} because those ranks are usually too broad for
evidence querying. They can still be used if explicitly included.}

\item{mode}{Extraction mode. \code{"highest"} returns the most specific valid
assignment per feature. \code{"all"} returns all valid assignments across
\code{ranks}.}

\item{include_feature_id}{Logical scalar. If \code{TRUE}, include a \code{feature_id}
column using an existing feature ID column where available, otherwise using
non-default taxonomy row names.}

\item{drop_uncultured}{Logical scalar. If \code{TRUE}, remove uninformative values
such as \code{"uncultured"}, \code{"unidentified"}, \code{"unclassified"}, \code{"unknown"},
\code{"environmental sample"}, \code{"metagenome"}, \code{"Incertae sedis"}, and empty
rank prefixes such as \code{"g__"} or \code{"s__"}.}

\item{drop_empty}{Logical scalar. If \code{TRUE}, remove missing, blank, and
literal \code{"NA"} values.}

\item{unique}{Logical scalar. If \code{TRUE}, deduplicate rows by \code{taxon_name} and
\code{taxon_rank}. When \code{feature_id} is present, the first feature ID for each
duplicate taxon/rank pair is retained; \code{feature_id} is not part of the
deduplication key.}
}
\value{
A tibble with \code{taxon_name} and \code{taxon_rank}, and optionally
\code{feature_id}.
}
\description{
Extracts taxon names from a taxonomy table or phyloseq/speedyseq object so
they can be queried against evidence sources such as WoRMS. This helper only
prepares query taxa. It does not call WoRMS, standardise taxon names, or make
retain/review/exclude decisions.

By default, the function returns unique taxa for evidence querying: the
highest available taxonomic assignment is chosen for each feature and
duplicate \code{taxon_name}/\code{taxon_rank} pairs are removed. Use
\code{include_feature_id = TRUE} and \code{unique = FALSE} to keep a feature-level
mapping table with one highest-resolution assignment per ASV/feature. Use
\code{mode = "all"} to return every valid assignment across the selected ranks.

\code{extract_taxa_for_evidence()} is intentionally separate from
\code{fetch_worms_evidence()}: first extract the taxa you want to query, then pass
the resulting table to an evidence retrieval function.
}
\examples{
tax_table <- data.frame(
  Kingdom = "Animalia",
  Phylum = "Chordata",
  Class = "Actinopteri",
  Family = "Salmonidae",
  Genus = "Salmo",
  Species = "Salmo salar"
)

taxa_to_query <- extract_taxa_for_evidence(tax_table)

data("ps_test_data_euk")
taxa_to_query <- extract_taxa_for_evidence(ps_test_data_euk)

\dontrun{
worms_evidence <- fetch_worms_evidence(
  taxa_to_query,
  by = "name",
  cache_path = "worms_cache.rds"
)
}
}
