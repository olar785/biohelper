% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_worms_evidence_db.R
\name{build_worms_evidence_db}
\alias{build_worms_evidence_db}
\title{Build a local WoRMS evidence database}
\usage{
build_worms_evidence_db(
  worms_taxonomy,
  output_path,
  id_col = NULL,
  name_col = NULL,
  rank_col = NULL,
  prefer = c("aphia_id", "name"),
  batch_size = 100,
  sleep = 0.2,
  resume = TRUE,
  overwrite = FALSE,
  marine_only = FALSE,
  checked_at = Sys.Date(),
  verbose = TRUE
)
}
\arguments{
\item{worms_taxonomy}{A data frame or path to a local WoRMS taxonomy file.
Supported file types are \code{.csv}, \code{.tsv}, \code{.txt}, \code{.rds}, and \code{.xlsx} when
the optional \code{readxl} package is installed.}

\item{output_path}{Path where the standard taxon evidence RDS file should be
written.}

\item{id_col}{Optional AphiaID-like column name. Defaults to the first
available of \code{AphiaID}, \code{aphia_id}, \code{taxonID}, \code{taxon_id}, or
\code{source_taxon_id}.}

\item{name_col}{Optional scientific-name column name. Defaults to the first
available of \code{ScientificName}, \code{scientificName}, \code{scientificname}, \code{name},
or \code{taxon_name}.}

\item{rank_col}{Optional taxon-rank column name. Defaults to the first
available of \code{taxonRank}, \code{rank}, or \code{taxon_rank}.}

\item{prefer}{Query preference. If \code{"aphia_id"}, AphiaID queries are used
when an AphiaID-like column is available, otherwise name queries are used.
If \code{"name"}, name queries are preferred when a name column is available.}

\item{batch_size}{Positive integer scalar giving the number of unique taxa to
query per batch.}

\item{sleep}{Non-negative numeric scalar. Seconds to pause between batches
and passed to \code{fetch_worms_evidence()} for AphiaID calls.}

\item{resume}{Logical scalar. If \code{TRUE} and \code{output_path} exists, read the
existing evidence database and query only taxa not already present.}

\item{overwrite}{Logical scalar. If \code{TRUE}, rebuild from scratch even when
\code{output_path} exists.}

\item{marine_only}{Logical scalar passed to \code{fetch_worms_evidence()} for name
queries.}

\item{checked_at}{Date or character scalar recorded in new evidence rows.}

\item{verbose}{Logical scalar. If \code{TRUE}, emit progress messages.}
}
\value{
A standard taxon evidence data frame, also saved to \code{output_path} as
an RDS file.
}
\description{
Builds a reusable local WoRMS evidence database from a local WoRMS taxonomy
table or file. This is intended for once-off or occasional local database
builds, not for downloading the full WoRMS database.

Users should obtain WoRMS taxonomy content according to the WoRMS terms of
use, then pass that local table to \code{build_worms_evidence_db()}. The function
enriches the supplied table by querying the WoRMS API through
\code{fetch_worms_evidence()} for taxonomic/environment evidence, saves the
standard evidence table as an RDS file, and can resume interrupted builds.

Large builds may take a long time. Use \code{batch_size}, \code{sleep}, and \code{resume = TRUE} to query gently and cache progress.
}
\examples{
\dontrun{
evidence <- build_worms_evidence_db(
  worms_taxonomy = "worms_taxonomy_export.csv",
  output_path = "worms_evidence_db.rds",
  batch_size = 100,
  resume = TRUE
)
}
}
