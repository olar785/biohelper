% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_taxa_metadata.R
\name{add_taxa_metadata}
\alias{add_taxa_metadata}
\title{Add taxon-level metadata to a phyloseq taxonomy table}
\usage{
add_taxa_metadata(
  ps,
  taxa_metadata,
  source = "summary",
  cols = NULL,
  prefix = "tm_",
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{ps}{A phyloseq object with a \code{tax_table()}.}

\item{taxa_metadata}{Either the full list returned by
\code{\link[=assess_taxa_evidence]{assess_taxa_evidence()}} or a single metadata data frame.}

\item{source}{Character scalar naming the list element to use when
\code{taxa_metadata} is a list. Defaults to \code{"summary"}.}

\item{cols}{Optional character vector of metadata columns to add. If \code{NULL},
all columns except \code{feature_id} are added.}

\item{prefix}{Character scalar prepended to added metadata columns. Defaults
to \code{"tm_"}.}

\item{overwrite}{Logical scalar. If \code{FALSE}, error when a target metadata
column already exists in \code{phyloseq::tax_table(ps)}. If \code{TRUE}, replace the
target metadata columns.}

\item{verbose}{Logical scalar. If \code{TRUE}, report how many columns were added.}
}
\value{
A standard phyloseq object with metadata columns appended to
\code{phyloseq::tax_table(ps)}.
}
\description{
\code{add_taxa_metadata()} attaches taxon-level metadata, such as the \code{summary}
table returned by \code{\link[=assess_taxa_evidence]{assess_taxa_evidence()}}, to the \code{tax_table()} of a
phyloseq object. The metadata are stored as additional taxonomy-table
columns, so they will appear in \code{phyloseq::rank_names(ps)}.

A short prefix is added to each metadata column to make these fields easy to
identify and remove. Use \code{\link[=drop_taxa_metadata]{drop_taxa_metadata()}} before taxonomy filtering or
merging steps that should only see true taxonomic ranks.

Richer evidence tables such as \code{details}, \code{worms}, \code{obis}, and \code{ecology} may
be better kept separately if they contain multiple rows per \code{feature_id}.
The selected table must contain at most one row per \code{feature_id}.
}
\examples{
otu <- phyloseq::otu_table(
  matrix(
    c(1, 0, 2, 3),
    nrow = 2,
    dimnames = list(c("asv1", "asv2"), c("sample1", "sample2"))
  ),
  taxa_are_rows = TRUE
)
tax <- phyloseq::tax_table(
  matrix(
    c("Metazoa", "Chordata", "Metazoa", "Arthropoda"),
    nrow = 2,
    byrow = TRUE,
    dimnames = list(c("asv1", "asv2"), c("kingdom", "phylum"))
  )
)
ps <- phyloseq::phyloseq(otu, tax)
metadata <- data.frame(
  feature_id = c("asv1", "asv2"),
  recommended_action = c("retain", "flag_for_review")
)

ps <- add_taxa_metadata(ps, metadata, verbose = FALSE)
phyloseq::rank_names(ps)
ps <- drop_taxa_metadata(ps)
}
