% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_taxa_evidence.R
\name{assess_taxa_evidence}
\alias{assess_taxa_evidence}
\title{Deterministically assess taxa against local evidence}
\usage{
assess_taxa_evidence(
  x,
  expected_environment,
  ...,
  expected_habitat_type = c("unspecified", "coastal_benthic", "deep_sea_benthic",
    "deep_sea_pelagic", "pelagic", "freshwater", "terrestrial", "host_associated"),
  expected_region = NULL,
  tax_ranks = c("species", "genus", "family", "order", "class", "phylum"),
  evidence_sources = NULL,
  taxon_evidence = NULL,
  taxon_evidence_path = NULL,
  ecology_evidence = NULL,
  ecology_evidence_path = NULL,
  obis_geometry = NULL,
  obis_region_label = NULL,
  obis_region_relation = c("direct", "supporting"),
  taxon_evidence_region_relation = c("direct", "supporting"),
  taxon_evidence_region_label = NULL,
  verbose = FALSE,
  worms_batch_size = 25,
  worms_max_tries = 3,
  worms_retry_sleep = 5
)
}
\arguments{
\item{x}{A taxonomy table as a data frame, matrix, phyloseq taxonomy table,
or a phyloseq/speedyseq object containing a taxonomy table.}

\item{expected_environment}{Character scalar describing the expected broad
environment, for example \code{"marine"}, \code{"freshwater"}, or \code{"terrestrial"}.}

\item{...}{Reserved for backward compatibility with earlier calls.}

\item{expected_habitat_type}{Optional character scalar describing the broad
habitat type used by deterministic ecology rules. Allowed values are
\code{"unspecified"}, \code{"coastal_benthic"}, \code{"deep_sea_benthic"},
\code{"deep_sea_pelagic"}, \code{"pelagic"}, \code{"freshwater"}, \code{"terrestrial"}, and
\code{"host_associated"}. \code{"deep_sea_benthic"} enables conservative treatment
of birds and reptiles as likely transient/allochthonous in deep-sea
benthic samples. If omitted or \code{NULL}, \code{"unspecified"} is used.}

\item{expected_region}{Optional character scalar describing the expected
geographic region.}

\item{tax_ranks}{Character vector of taxonomic ranks to inspect, ordered
from most specific to broadest.}

\item{evidence_sources}{Optional character vector of on-demand evidence
sources to fetch or use before deterministic assessment. Currently
\code{"worms"} and \code{"obis"} can trigger on-demand queries when their other
inputs are supplied; \code{"ecology"} is accepted as an explicit local ecology
evidence layer when \code{ecology_evidence} or \code{ecology_evidence_path} is
supplied. Use \code{NULL} to avoid automatic evidence retrieval.}

\item{taxon_evidence}{Optional data frame containing user-supplied evidence
about taxon environment, habitat, and/or region, including output from
\code{fetch_worms_evidence()}.}

\item{taxon_evidence_path}{Optional path to a locally stored taxon evidence
table. Standard \code{taxon_evidence} tables are read directly; CCZ-like WoRMS
checklist files are standardised with \code{read_worms_ccz_evidence()}. This
never downloads data.}

\item{ecology_evidence}{Optional FishBase/SeaLifeBase ecology evidence table,
usually \code{build_fishbase_sealifebase_ecology_db()$combined}, or the full
list returned by \code{build_fishbase_sealifebase_ecology_db()}. This evidence
is used only for habitat/ecology interpretation and output metadata. It
never changes \code{expected_environment_status} or
\code{expected_region_status}.}

\item{ecology_evidence_path}{Optional path to a locally stored
FishBase/SeaLifeBase ecology evidence table or RDS list containing a
\code{combined} element. Supported formats are \code{.csv}, \code{.tsv}, \code{.txt}, \code{.rds},
\code{.xlsx}, and \code{.xls} when \code{readxl} is available.}

\item{obis_geometry}{Optional WKT geometry used for OBIS regional checklist
queries. When \code{evidence_sources} includes \code{"obis"}, this function calls
\code{robis::checklist(scientificname = query_name, geometry = obis_geometry)}
for each unique query taxon. Raw OBIS occurrences, depth summaries, and
caching are not used.}

\item{obis_region_label}{Optional label for \code{obis_geometry}. If \code{NULL}, the
label defaults to \code{expected_region} when available, otherwise
\code{"obis_geometry"}.}

\item{obis_region_relation}{Character scalar, either \code{"direct"} or
\code{"supporting"}. Use \code{"direct"} when \code{obis_geometry} is the exact expected
region and OBIS checklist records should support \code{known_in_region}. Use
\code{"supporting"} when the geometry is broader or contextually relevant and
records should support \code{plausible_in_region}.}

\item{taxon_evidence_region_relation}{Character scalar, either \code{"direct"} or
\code{"supporting"}. Use \code{"direct"} when user-supplied \code{taxon_evidence} is from
the exact expected region. Use \code{"supporting"} when user evidence is from a
broader or related region.}

\item{taxon_evidence_region_label}{Optional label for the region or context
represented by user-provided \code{taxon_evidence} or \code{taxon_evidence_path}.
When \code{NULL}, the region is inferred from matched evidence rows when
possible.}

\item{verbose}{Logical scalar. If \code{TRUE}, report progress messages.}

\item{worms_batch_size}{Positive integer scalar. Maximum number of unique
taxa sent to WoRMS per live query batch when on-demand WoRMS evidence is
requested. Defaults to \code{25}.}

\item{worms_max_tries}{Positive integer scalar. Maximum number of attempts
for transient WoRMS/API failures per batch. Defaults to \code{3}.}

\item{worms_retry_sleep}{Non-negative numeric scalar. Seconds to wait between
WoRMS retry attempts. Defaults to \code{5}.}
}
\value{
A list with \code{summary}, \code{details}, \code{worms}, \code{obis}, and \code{ecology}
elements. \code{summary} is a compact tibble with one row per original input
feature/taxon row and key columns such as \code{feature_id}, \code{taxon_name},
\code{taxon_rank}, \code{lineage}, \code{worms_environment},
\code{ecology_evidence_habitat_broad}, \code{ecology_evidence_depth_zone},
\code{ecology_evidence_depth_min}, \code{ecology_evidence_depth_max},
\code{obis_region_status}, deterministic status columns, \code{recommended_action},
short \code{rationale}, and \code{references}. It does not include internal
\code{query_id}, redundant \code{ecology_evidence_position}, dominant ecology
columns, or OBIS compact rank columns \code{obis_n_rank}/\code{obis_taxa_rank}.
\code{details} is the cleaned wide audit table with the longer rationale and
fuller evidence summaries. The source-specific tables contain the columns
that are available for that run and are empty tibbles when no columns are
available.
}
\description{
\code{assess_taxa_evidence()} runs the evidence-first, deterministic part of the
\code{flag_taxa()} workflow. It extracts the highest useful taxonomic assignment
for each input feature, matches user-supplied or locally read evidence, and
returns environment, habitat, region, ecological, occurrence, and recommended
action fields.

Missing evidence, failed lookups, no WoRMS match, absent CCZ records, and
absent habitat or region records are treated as uncertainty rather than as
positive incompatibility evidence. OBIS checklist results are used as
regional occurrence evidence only: no OBIS records means missing regional
evidence, not evidence of absence, and OBIS is not treated as proof of
resident habitat use.

\code{expected_habitat_status = "no_known_habitat_evidence"} means an expected
habitat was supplied, but deterministic evidence sources did not document the
taxon in that habitat. \code{expected_habitat_status = "unknown"} is reserved for
unassessed or unusable habitat context. Missing habitat evidence triggers
review rather than exclusion unless positive incompatibility evidence is
present. A retained row means available deterministic evidence supports
plausibility and no positive incompatibility was detected; it does not mean
every ecological detail is proven.

Environment compatibility is assessed from trusted local evidence first:
user-provided taxon evidence, then WoRMS-derived environment flags when
requested. OBIS is primarily a regional occurrence layer. OBIS environment
flags are used only as fallback environment evidence when no matched
user/WoRMS environment evidence is available, and this fallback is noted in
the output. Terrestrial-only or freshwater-only trusted evidence in an
expected marine sample is treated as a clear environment incompatibility and
may be excluded deterministically. OBIS regional records do not override
trusted environment incompatibility.

User-provided evidence and OBIS checklist evidence are kept as separate
evidence layers in the output. Both can contribute to
\code{expected_region_status} using the priority \code{known_in_region} >
\code{plausible_in_region} > \code{no_distribution_evidence}.

FishBase/SeaLifeBase ecology evidence, when supplied through
\code{ecology_evidence} or \code{ecology_evidence_path}, is a habitat/ecology metadata
layer only. It may refine \code{expected_habitat_status},
\code{ecological_status}, \code{occurrence_interpretation},
\code{recommended_action}, \code{rationale}, and \verb{ecology_evidence_*} output columns,
but it does not influence \code{expected_environment_status} or
\code{expected_region_status}.

The function always returns a list. \code{res$summary} is a compact table for
reviewing taxa: taxon identity, lineage, key WoRMS/OBIS/ecology summaries,
then the deterministic status/action columns near the end. \code{res$details} is
the cleaned wide audit table. \code{res$worms}, \code{res$obis}, and \code{res$ecology}
contain source-specific evidence slices and are empty tibbles when no columns
are available for that source.

The public ecology habitat field is \code{ecology_evidence_habitat_broad}, derived
from FishBase/SeaLifeBase broad habitat metadata. Redundant
\code{ecology_evidence_position} values are not shown in the user-facing outputs.
Aggregated semicolon-separated ecology terms are de-duplicated, and dominant
ecology fields in the detailed/audit tables contain a single value plus a
proportion. The summary keeps only compact ecology fields, including min/max
depth, and uses a short one-sentence rationale rather than the accumulated
audit rationale.
In \code{expected_habitat_type = "deep_sea_benthic"} context, Aves and Reptilia
are flagged as possible transient/allochthonous signals rather than resident
benthic organisms. Mammalia are not blanket-flagged by this rule because some
marine mammals dive deeply.
}
\examples{
tax <- data.frame(
  phylum = "Chordata",
  family = "Salmonidae",
  genus = "Salmo",
  species = "Salmo salar"
)

assess_taxa_evidence(
  tax,
  expected_environment = "marine",
  expected_habitat_type = "coastal_benthic"
)

\dontrun{
# With a local FishBase/SeaLifeBase ecology DB and optional regional evidence:
res <- biohelper::assess_taxa_evidence(
  x = ps,
  expected_environment = "marine",
  expected_habitat_type = "deep_sea_benthic",
  expected_region = "Clarion-Clipperton Zone",
  evidence_sources = c("worms", "obis", "ecology"),
  ecology_evidence = fb_slb_db$combined
)

res$summary
res$details
res$obis
res$ecology
}
}
