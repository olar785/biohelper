% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_worms_evidence.R
\name{fetch_worms_evidence}
\alias{fetch_worms_evidence}
\title{Fetch WoRMS taxon evidence for selected taxa}
\usage{
fetch_worms_evidence(
  taxa,
  by = c("name", "aphia_id"),
  marine_only = FALSE,
  cache_path = NULL,
  sleep = 0.2,
  checked_at = Sys.Date(),
  verbose = TRUE
)
}
\arguments{
\item{taxa}{Character vector of taxon names when \code{by = "name"}, or an
integer, numeric, or character vector of AphiaIDs when \code{by = "aphia_id"}.
AphiaIDs are normalised to numeric values before querying WoRMS.}

\item{by}{Query mode. Use \code{"name"} to query taxon names with
\code{worrms::wm_records_names()}, or \code{"aphia_id"} to query AphiaIDs with
\code{worrms::wm_record()}.}

\item{marine_only}{Logical scalar passed to \code{worrms::wm_records_names()} when
\code{by = "name"}.}

\item{cache_path}{Optional path to an RDS cache. Existing cached rows are
reused and new query results are appended.}

\item{sleep}{Numeric scalar. Seconds to pause between repeated AphiaID
requests.}

\item{checked_at}{Date or character scalar recorded in the \code{checked_at}
column.}

\item{verbose}{Logical scalar. If \code{TRUE}, emit short progress messages.}
}
\value{
A data frame in the standard taxon evidence format.
}
\description{
Queries WoRMS for a supplied list of taxon names or AphiaIDs and returns the
standard taxon evidence format used by \code{flag_taxa()} and
\code{build_taxon_evidence()}. This function does not download or build the full
WoRMS database. It is intended for enriching a small set of taxa and for
building a reusable local WoRMS evidence cache.

For larger enrichment runs, query gently, use \code{sleep} where repeated AphiaID
requests are needed, and provide \code{cache_path} so successful results can be
reused across sessions.
}
\details{
The \code{worrms} package is a suggested dependency. If it is not installed and a
live query is needed, \code{fetch_worms_evidence()} errors with an installation
message. Tests and examples should mock WoRMS responses or use cached data
rather than making live API calls.
}
\examples{
\dontrun{
evidence <- fetch_worms_evidence(
  taxa = c("Salmo salar", "Gadus morhua"),
  by = "name",
  cache_path = "worms_evidence_cache.rds"
)
}
}
