% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_taxa.R
\name{apply_flag_taxa_llm_result}
\alias{apply_flag_taxa_llm_result}
\title{Apply a manual flag_taxa LLM result}
\usage{
apply_flag_taxa_llm_result(result, llm_result = NULL, llm_json_path = NULL)
}
\arguments{
\item{result}{Evidence-first result table returned by
\code{flag_taxa(prompt_only = TRUE)}.}

\item{llm_result}{Optional data frame or list parsed from JSON. Supply either
\code{llm_result} or \code{llm_json_path}.}

\item{llm_json_path}{Optional path to a JSON file containing an array of LLM
result objects. Read with \code{jsonlite::fromJSON()}.}
}
\value{
A \code{data.frame} in the same final output format as
\code{flag_taxa(prompt_only = FALSE)}, with LLM/manual judgements merged into
rows where \code{llm_selected = TRUE} and deterministic rows preserved.
}
\description{
Integrates a structured JSON/data-frame result from a manually answered
\code{flag_taxa(prompt_only = TRUE)} prompt into the evidence-first table returned
by that call.
}
\examples{
tax <- data.frame(
  phylum = "Chordata",
  genus = "Salmo",
  species = "Salmo salar"
)

evidence_first <- flag_taxa(
  tax,
  expected_environment = "marine",
  prompt_only = TRUE,
  prompt_path = tempdir()
)

# After manually saving a valid JSON response:
# final <- apply_flag_taxa_llm_result(
#   evidence_first,
#   llm_json_path = "flag_taxa_llm_result.json"
# )
}
